# backend (Express, :5000)
MONGODB_URI=
GEMINI_API_KEY=
JWT_SECRET=
FACE_MATCH_THRESHOLD=0.5
PORT=5000
FRONTEND_ORIGIN=http://localhost:5173

# server/ (live detection, :5001) — set these in that process's environment
STREAM_URL=
AI_LOG_FILE=
